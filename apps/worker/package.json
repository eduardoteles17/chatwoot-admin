{
  "name": "@chatwoot-admin/worker",
  "private": true,
  "scripts": {
    "dev": "dotenvx run -f ../../.env -f .env -- bun run ./src/main.ts"
  },
  "devDependencies": {
    "@chatwoot-admin/tsconfig": "workspace:*"
  }
}
